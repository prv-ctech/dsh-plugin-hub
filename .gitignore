node_modules/
*.tgz
*.log
*.tsbuildinfo
.DS_Store
.env
.env.*
.npmrc
.npmrc.*
coverage/
# Internal release templates; keep them out of the repository.
docs/release-notes.template.md
# Internal issue reply templates and drafts; keep them local.
docs/GitHub Issue 回复模板.md
docs/回复输出.md
docs/issue-reply-*.md
docs/issue-*-reply.md
# Build outputs are recreated on each build. Root anchors prevent `client/` from
# also ignoring `src/client/`. Tracked outputs remain tracked; force-add new
# runtime outputs when preparing a release.
/lib/
/client/
